$('#comment_content').val("");

$('.modal-comment').append(`<b><%= @comment.user.name %></b> | <%= @comment.content %> <%= link_to 'X', post_comment_path(@post, @comment), method: :delete, data: { confirm: 'Are you sure?' }, :remote => true %><br>`);
